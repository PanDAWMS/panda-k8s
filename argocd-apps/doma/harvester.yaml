apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: panda-harvester
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/PanDAWMS/panda-k8s.git
    targetRevision: main
    path: helm/harvester
    helm:
      valueFiles:
        - values.yaml
        - values/values-cern.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: apps
      kind: StatefulSet
      name: panda-harvester
      jsonPointers:
        - /spec/volumeClaimTemplates
    - group: ""
      kind: PersistentVolumeClaim
      name: panda-harvester-wdirs
      jsonPointers:
        # set by the PV controller once bound; never present in the chart's
        # own PVC spec, so it always shows as drift otherwise
        - /spec/volumeName
